import { State } from "./types";

export const state: State = {
  // Global:
  currentAuth: null,
  // Quiz Builder:
  // -- Show Mode:
  currentQuiz: null,
  // -- Create Mode:
  currentCreatedQuiz: null
};
